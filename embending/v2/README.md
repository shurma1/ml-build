# v2 — поиск по услугам МФЦ Тульской области

Поиск услуги по расшифровке разговора оператора с посетителем.
732 карточки → 318 типов услуг, муниципалитет как фасет, право на услугу как
фильтр поверх поиска.

| документ | о чём |
|---|---|
| **[PIPELINE.md](PIPELINE.md)** | как работает, как запустить, как переиспользовать |
| **[DATA.md](DATA.md)** | что сделали с данными и зачем, с числами |

```bash
export EMB_DB="host=localhost port=5434 dbname=embeddings user=emb password=emb"
export EMB_MODEL="intfloat/multilingual-e5-large-instruct"

python3 v2/build_index.py          # ~35 с
python3 v2/eval/acceptance.py      # 22 проверки
python3 v2/search.py "потерял паспорт в щекино"
```

Качество на 690 запросах: **R@1 0.732**, R@10 0.977, MRR 0.817.
С двумя уточняющими вопросами — R@1 0.810, с четырьмя — 0.839.
Задержка на CPU: 120 мс холодно, 7–9 мс из кэша.

Ограничения и то, что проверено и отвергнуто, — в конце обоих документов.
